name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

# Opt-in to Node.js 24 runtime for JavaScript actions (Node.js 20 is being
# deprecated; see https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from manifest
        id: ver
        run: |
          ver=$(grep -E '^version\s*=' src/kinema/blender_manifest.toml | head -1 | sed -E 's/^version\s*=\s*"([^"]+)".*/\1/')
          echo "version=$ver" >> $GITHUB_OUTPUT
          echo "Building kinema v$ver"

      - name: Build ZIP
        run: |
          mkdir -p dist staging
          rsync -a --exclude='__pycache__' --exclude='*.pyc' --exclude='*.pyo' \
                src/kinema/ staging/kinema/
          cd staging
          zip -r "../dist/kinema-v${{ steps.ver.outputs.version }}.zip" kinema
          cd ..
          ls -lh dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/kinema-v${{ steps.ver.outputs.version }}.zip
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
